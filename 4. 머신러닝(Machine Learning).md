### 애플리케이션 사례 `핸즈온머신러닝 32p`
- 생산라인에서 제품 이미지를 분석해 자동으로 분류하기
- 뇌를 스캔하여 종양 진단하기
- 자동으로 뉴스 기사 분류하기
- 토론 포럼에서 부정적인 코멘트를 자동으로 구분하기


### 머신러닝

컴퓨터에게 모든 판단 규칙을 사람이 직접 프로그래밍하는 대신, **데이터를 주고 그 데이터에서 규칙이나 패턴을 스스로 학습하게 하는 기술**입니다.
즉, 사람이 규칙을 직접 작성하기보다 **컴퓨터가 데이터에서 규칙을 찾아 모델을 만드는 것**입니다.

```
데이터 + 정답
     ↓
머신러닝 알고리즘
     ↓
   학습 (특징(Feature) 추출)
     ↓
 모델(Model) 생성
     ↓
새로운 데이터 → 예측
```

> **모델 = 데이터를 학습해서 만들어진 판단/예측 함수** 

```
데이터
 ↓
전처리
 ↓
학습 데이터 / 테스트 데이터
 ↓
알고리즘 선택
 ↓
학습 (fit)
 ↓
모델
 ↓
예측 (predict)
 ↓
성능 평가
```

##### 머신러닝 기법

- [[#지도학습]] (supervied learning)         : 배움
	- 분류(classification)
	- 회귀(regression)
- [[#비지도학습]] (unsupervied learning)
	- 군집화(clustering)
	- 변환(transform)
	- 연관(assocation)
- [[#강화학습]] (reinfocement learning )   : 경험

```
                    머신러닝 / 딥러닝
                          │
          ┌───────────────┼───────────────┐
          ↓               ↓               ↓
       지도학습        비지도학습        강화학습
          │               │               │
        분류            군집화          Q-Learning
        회귀            PCA             DQN
                          │
                     생성 모델
                     GAN / VAE
```


###### 군집과 분류의 차이
![(null)](file:///C:\Users\admin\AppData\Local\Temp\cif00001.png)

### 지도학습

**개념**
- 훈련 데이터로부터 하나의 함수를 유추해내기 위한 기계학습 기법

**특징**
- 명시적인 정답이 주어진 상황에서 기계를 학습시키기 위해 데이터와 레이블을 사용

**기법**
- 분류
	- 이진분류
	- 다중분류
- 회귀
	- 독립변수 기반 분석
	- 종속적인 기반 분석

**알고리즘**
- 인공신경망 측면
	- CNN
	- RNN
- 벡터 기반 측면
	- SVM
	- Regeression

### 비지도학습

**비지도학습(Unsupervised Learning)** 은 **정답(Label)을 주지 않고 데이터만 제공해서, 컴퓨터가 데이터 속의 패턴이나 구조를 스스로 찾아내도록 하는 머신러닝 방법**입니다.


**알고리즘**
- 군집화(Clustering) : 비슷한 데이터를 묶는 것
	- K-Means
	- DBSCAN
- 차원축소(Dimensionality Reduction) / 변환(transformation)
	- PCA
- 연관 규칙(assosication)
	- Apriori

**K-Means**
로봇의 센서 데이터를 군집화하여 사람이 결과 분석
```
센서 데이터
    ↓
 K-Means
    ↓
┌────────┬────────┬────────┐
│ 그룹 A │ 그룹 B │ 그룹 C │
├────────┼────────┼────────┤
│패턴 1  │패턴 2  │패턴 3  │
└────────┴────────┴────────┘
   ↓
컴퓨터는 이 그룹이 무엇을 의미하는지는 모릅니다. 사람이 결과를 분석한 후 "A는 정상 주행, B는 회전 상황, C는 장애물 접근 상황과 비슷하구나"라고 의미를 해석
```

**차원축소**
데이터의 중요한 정보를 최대한 유지하면서 변수의 개수를 줄이는 것
```
원래 데이터

속도
거리
각도
밝기
온도
진동
압력
...
100개의 특징
     ↓
    PCA
     ↓
10개의 중요한 정보로 압축
```

**자율주행 로봇에서 비지도학습 활용**
```
Camera / LiDAR
       ↓
수많은 데이터 포인트
       ↓
   Clustering
       ↓
비슷한 데이터끼리 그룹화
       ↓
장애물 / 주변 환경 구조 분석
```
특히 LiDAR의 점(Point Cloud)을 공간적으로 묶어 **서로 다른 물체 후보를 분리**하는 데 DBSCAN 같은 군집화 개념을 적용할 수 있습니다.


**지도학습과 비지도학습의 차이**

>**지도학습 = 문제 + 정답으로 학습**  
>**비지도학습 = 문제만 주고 데이터의 구조나 패턴을 발견**

![[지도vs비지도.png]]


### 강화학습(Reinforcement Learning)

**Agent가 Environment에서 행동(Action)을 해보고, 그 결과로 받은 Reward를 이용해 더 좋은 행동 방법(Policy)을 학습하는 머신러닝 방법**입니다.


> **`State → Policy → Action → Reward → Policy 개선`**
> 
> Agent(로봇)가 Environment(도로)에서 **State**(현재 상황)를 보고 **Policy**(행동 전략)에 따라 **Action**(조향·속도)을 선택하고, **Reward**(보상)를 받으면서 더 좋은 주행 방법을 학습한다.


```
                Action
          ┌──────────────→
          │
       Agent                  Environment
     자율주행 로봇               도로/공장
          ↑                         │
          │                         │
          └──── State + Reward ─────┘
```

**1. agent** 
학습하고 행동하는 주체입니다. 자율주행에서는 로봇 또는 로봇의 주행 제어 AI가 Agent가 됩니다.

**2. Environment(환경)**
Agent가 행동하는 세계입니다. 로봇이라면 도로, 공장 바닥, 장애물, 다른 로봇 등이 환경에 해당합니다.

**3. State(상태)**
Agent가 현재 환경을 파악한 정보. ision 기반 로봇에서는 카메라 영상 자체 또는 CNN으로 영상에서 추출한 특징이 State의 일부가 될 수도 있습니다.

예시) 앞 장애물 거리, 왼쪽 장애물 거리, 오른쪽 장애물 거리, 현재 속도, 목적지방향

**4. Action(행동)**
Agent가 현재 State를 보고 선택할 수 있는 행동
예시) 앞에 장애물이 있다면 Agent가 `좌회전`이라는 Action을 선택

**5. Reward(보상)**
Agent가 선택한 행동이 얼마나 좋았는지를 숫자로 알려주는 신호
강화학습에서는 **정답을 직접 알려주는 대신 보상을 줍니다.**

**6. Policy(정책)**
가장 중요한 개념 중 하나입니다. **현재 State에서 어떤 Action을 선택할지를 결정하는 전략**


### 머신러닝 vs 딥러닝

AI (Artificial Intelligence:인공지능)
- Machine Learning (기계학습)
	- Linear Regression(선형회귀)
	- Decision Tree
	- Random Forest
	- SVM
	- [[5. 딥러닝(Deep Learning)|딥러닝]]
		- DNN
		- CNN
		- RNN
		- Transformer


머신러닝의 한 분야로, **인공신경망(Artificial Neural Network)** 을 여러 층으로 깊게 쌓아서 데이터의 특징을 컴퓨터가 스스로 학습하는 방법

| 구분          | 머신러닝                     | 딥러닝                   |
| ----------- | ------------------------ | --------------------- |
| 관계          | AI의 한 분야                 | 머신러닝의 한 분야            |
| 특징 추출       | 사람이 많이 관여. **수동**으로 추출   | 모델이 **자동**으로 추출       |
| 대표 모델       | 선형회귀, SVM, Random Forest | CNN, RNN, Transformer |
| 데이터         | 비교적 적어도 가능               | 일반적으로 많은 데이터가 유리      |
| 연산량         | 상대적으로 적음                 | 큼                     |
| GPU         | 필수 아님                    | 학습 시 많이 사용            |
| 이미지 처리      | 가능하지만 특징 설계가 중요          | 매우 강력                 |
| 자율주행 Vision | 일부 판단/센서 분석              | 객체·차선·영상 인식의 핵심       |

### 생성모델
- Autoencoder
- VAE
- [[#GAN]]
- Diffusion Model
- Transformer 기반 생성 모델


### GAN
- [강화학습을 이용해서 자동차의 주차능력을 향상시키는 예제](https://www.youtube.com/watch?v=VMp6pq6_QjI)
- [시뮬레이션 환경을 이용한 숨바꼭질 강화학습 영상](https://youtu.be/kopoLzvh5jY)
- [유니티 게임 머신 러닝](https://www.youtube.com/watch?v=fiQsmdwEGT8)
- [강화학습을 통해 로봇 팔이 문 여는 방법을 터득하는 영상](https://youtu.be/ZhsEKTo7V04)
- [2D 시뮬레이터 상에서 강화학습을 이용해 자율주행 기능을 구현하는 영상](https://www.youtube.com/watch?v=Aut32pR5PQA)
- [인공신경망과 강화학습을 이용해 플래피 버드 게임을 하는 인공지능을 만드는 영상](https://www.youtube.com/watch?v=WSW-5m8lRMs&t=357s)
- 

-----

#### 프레임워크

- TensorFlow
	- 구글에서 만든 딥러닝 엔진(프레임워크)
	- GPU 연산, 자동미분(Backpropagation), 텐서연산, 모델학습, 모델배포
- Keras
	- TensorFlow를 쉽게 사용하는 인터페이스(고수준 API)
	- 복잡한 신경망을 간단하게 만들 수 있음
	- tensorflow.keras.models
- PyTorch : TensorFlow와 경쟁하는 또 다른 딥러닝 프레임워크. Meta(Facebook)

TensorFlow와 PyTorch는 딥러닝 프레임워크이고, Keras는 TensorFlow 위에서 동작하는 고수준 API입니다.

##### 관계도
![[Pasted image 20260916160705.png|585]]

#### 자율주행에서는 무엇을 많이 사용할까?

사용자가 공부 중인 Vision 기반 로봇 자율주행에서는 다음과 같은 경향이 있습니다.

|분야|많이 사용하는 프레임워크|
|---|---|
|컴퓨터 비전(OpenCV)|PyTorch|
|YOLO|PyTorch|
|Detectron2|PyTorch|
|ROS AI 노드|PyTorch|
|TensorFlow Lite|임베디드 배포|
|NVIDIA Jetson|PyTorch 많이 사용|

최근 자율주행 연구에서는 PyTorch가 더 많이 사용되는 편입니다.

- 정적 그래프 방식보다 디버깅과 실험 반복이 쉬운 PyTorch가 유리
	- **동적 계산 그래프(dynamic computation graph, define-by-run)** 방식을 사용해서, 모델 구조를 코드 실행 중에 유연하게 바꿀 수 있습니다.
- 디버깅 편의성
- 학계·오픈소스 생태계 우위
	- 자율주행은 컴퓨터 비전(CV), 3D 인지, LiDAR 처리 등과 밀접한데, 이 분야의 최신 논문·오픈소스 코드(Detectron2, MMDetection3D, OpenPCDet, nuScenes devkit 등)가 압도적으로 PyTorch 기반으로 공개
- LiDAR 기반 3D 객체 탐지에 필수적인 라이브러리들(3D/포인트클라우드 처리) 지원


----

## scikit Learn (sklearn)

[머신러닝 문제 해결 가이드 및 알고리즘 추천](https://scikit-learn.org/stable/machine_learning_map.html)

![[ml_map.svg]]

좀 오래된 GTCC로 정리해 두고 있는데 초보자들이 따라가기에는 충분한 내용을 담고 있습니다. 내용을 보게 되면 일단 나 저 본인에게 머신러닝 문제가 주어졌을 때 스타트로부터 시작을 해서 데이터의 개수가 50개 이하라면 어 더 많은 데이터를 수집하라라고 안내를 해 주고 싶고요.

이탈리아 개수가 50개 이상이 되어지면 어 범주를 예측하는 문제인지를 먼저 질문을 합니다. 범주를 예측하는 질문인 경우에는 라벨 데이터 라벨이라는 의미는 어 정답이 있는 데이터라는 걸 의미합니다. 정답이 있는 데이터인 경우에는 결국 프랜시피테이션 지도 학습의 물류 문제에 해당할 거고요. 

물류 문제인 경우에 데이터 개수가 10만 개보다 작은 경우라면 설명 svc 알고리즘을 사용하고 어 만약에 텍스트 데이터다 그러면 라이브 베이즈를 통해서 문제를 해결해라라고. 안내를 하고 있습니다. 버스 데이터가 아닌 일반적인 크리에이티브의 데이터라고 하면 K  네이버 클래스파이어 즉 가까운 K계의 이웃으로 분류를 하는 알고리즘을 사용하고요. 

잘 동작하지 않는 경우라면 커널링 기법을 이용하는 SVC를 사용하라고 안내를 해 주고 있습니다. 만약에 데이터 개수가 10만 개보다 넘다. 그러면 이제 스토캐스트 그레이드 이센트 클래시파이어를 이용해서 사용하고요. 

이게 잘 동작하지 않는다면 어 커널 분석법을 이용해서 분류를 하도록 하는데 사실 어 라밴드 데이터가 아닌 카테고리를 예측하는 문제인 경우에는 이제 클러스터링 즉 비지도 학습의 문제가 되겠고요. 이 경우에 카테고리의 개수가 알려져 있는 경우에는 주로 케임인지 알고리즘을 사용하도록 하고요. 그렇지 않은 경우 우리가 메인 시프트나 아니면 다른 방법을 통해서 범죄를 예측하도록 사실 그 범죄를 예측하는 문제가 아닌 경우는 경우라면 자 수치를 예측하는 문제냐 하는 경우 수치를 예측하는 문제라고 하게 되면 이제 레그레이션 문제에 해당하고요. 

레그레이션 문제는 각성 예측하는 지도 학습의 핵이 문제인 것이죠. 훨씬 데이터 개수가 10만 개보다 작다. 그러면 우리가 몇 개의 중요한 변수들만이 우리가 예측할 수 있는데 중요한 요소로 우리가 잡을 것이냐 하는 경우라면 라솝 또는 일라스틱 알고리즘을 사용하도록 안내하고 있고요 만약에 이것이 이제 그런 문제가 아니다. 

이찌레그레이션을 이용해서 정확도 위주로 우리가 회계 모델을 구성하게 됩니다. 또는 서포트 벡터 레ble에서 터널을 가지고 있는 서포트 벡터 레글에서를 사용하거나 아니면 앙상블 레그레이션 같은 방법을 써서 좀 더 정확도 높은 모델을 구성할 수가 있습니다. 역시 마찬가지로 데이터의 개수가 10만 개보다 더 많은 경우에는 스토캐스트 그리디언트라 그랬어. 

이거를 수행화로 안내를 하고 있지요? 자 이제 여기서 우리가 범주를 예측하는 문제도 아니고 수치를 예측하는 문제도 아니고 반시 그 데이터를 갖다 보도록 하는 거다. 데이터가 어떤 모양을 갖고 있는지 알고 싶다. 

이런 경우라면 우리가 어 선거 분석 같은 것들을 이용하고요. 알겠습니다. 분석을 이용했는데 우리가 원하는 정도의 결과를 얻지 못하고 있다. 

이렇게 타겟 개수가 10만 개보다 작다. 이건 이제 우리가 역시 커널 어퍼시메이션이나 또는 아이소맵 같은 것들을 이용해서 데이터를 분석하라고 안내를 하고 있습니다. 
