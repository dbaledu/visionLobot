## 차선 인식 프로그램

핵심 흐름은 이것만 기억하면 됩니다.

```
원본 이미지
  ↓
Grayscale
  ↓
Gaussian Blur
  ↓
Canny Edge
  ↓
ROI(관심영역)
  ↓
Hough Transform
  ↓
차선 표시
```

아래 예제는 Windows + Python 기준이고, Colab에서도 거의 동일하게 사용할 수 있습니다.

## 1. OpenCV 설치

터미널에서:

```
pip install opencv-python numpy matplotlib
```

설치 확인:

```
import cv2

print(cv2.__version__)
```

---

## 2. 실습용 이미지 준비

먼저 프로젝트 폴더를 이렇게 만드세요.

```
lane_project/
│
├─ lane_detection.py
└─ road.jpg
```

`road.jpg`는 도로 앞쪽을 바라본 사진이면 됩니다.

가급적 이런 사진이 좋습니다.

```
        도로 멀리
           ▲
          / \
         /   \
        /     \
       /       \
      /         \
─────/───────────\─────
    왼쪽차선     오른쪽차선
```

처음에는 **흰색 차선이 선명하고 직선 도로인 이미지**가 좋습니다.

---

# 3. 먼저 이미지 읽기

`lane_detection.py`

```
import cv2

img = cv2.imread("road.jpg")

print(img.shape)

cv2.imshow("Original", img)

cv2.waitKey(0)
cv2.destroyAllWindows()
```

실행:

```
python lane_detection.py
```

이미지가 뜨면 성공입니다.

---

# 4. Grayscale 변환

컬러 영상은 BGR 3채널입니다.

```
B + G + R
```

차선을 찾을 때는 색상 정보가 꼭 필요하지 않기 때문에 흑백으로 변환합니다.

```
import cv2

img = cv2.imread("road.jpg")

gray = cv2.cvtColor(
    img,
    cv2.COLOR_BGR2GRAY
)

cv2.imshow("Original", img)
cv2.imshow("Gray", gray)

cv2.waitKey(0)
cv2.destroyAllWindows()
```

여기서 핵심 함수는:

```
cv2.cvtColor()
```

입니다.

---

# 5. Gaussian Blur 적용

카메라 영상에는 작은 노이즈가 많이 있습니다.

그래서 이미지를 약간 흐리게 합니다.

```
blur = cv2.GaussianBlur(
    gray,
    (5, 5),
    0
)
```

전체 코드는:

```
import cv2

img = cv2.imread("road.jpg")

gray = cv2.cvtColor(
    img,
    cv2.COLOR_BGR2GRAY
)

blur = cv2.GaussianBlur(
    gray,
    (5, 5),
    0
)

cv2.imshow("Gray", gray)
cv2.imshow("Blur", blur)

cv2.waitKey(0)
cv2.destroyAllWindows()
```

`(5, 5)`는 블러를 적용하는 Kernel 크기입니다.

처음에는 그냥:

```
(5, 5)
```

로 기억해도 충분합니다.

---

# 6. Canny Edge Detection

이제 차선의 경계선을 찾습니다.

```
edges = cv2.Canny(
    blur,
    50,
    150
)
```

전체:

```
import cv2

img = cv2.imread("road.jpg")

gray = cv2.cvtColor(
    img,
    cv2.COLOR_BGR2GRAY
)

blur = cv2.GaussianBlur(
    gray,
    (5, 5),
    0
)

edges = cv2.Canny(
    blur,
    50,
    150
)

cv2.imshow("Original", img)
cv2.imshow("Edges", edges)

cv2.waitKey(0)
cv2.destroyAllWindows()
```

결과는 보통 검은 화면에 흰 선들이 나타납니다.

```
원본

        /      \
       /        \
      /          \


Canny 결과

        /      \
       /        \
      /          \
```

하지만 문제점이 하나 있습니다.

도로뿐 아니라

```
자동차
건물
나무
가드레일
표지판
```

경계선도 모두 검출됩니다.

그래서 다음 단계가 필요합니다.

---

# 7. ROI: 관심 영역 설정

ROI는 **Region Of Interest**, 즉 우리가 관심 있는 영역만 사용하는 것입니다.

차선은 일반적으로 화면 아래쪽에 있습니다.

```
┌────────────────────┐
│                    │
│      하늘/건물      │
│                    │
│         /\         │
│        /  \        │
│       /    \       │
│████████████████████│
└────────────────────┘
       ↑
   관심 영역
```

삼각형 또는 사다리꼴 부분만 사용합니다.

먼저 NumPy를 추가합니다.

```
import numpy as np
```

ROI 함수를 만들어봅니다.

```
def region_of_interest(image):

    height = image.shape[0]
    width = image.shape[1]

    polygon = np.array([
        [
            (0, height),
            (width // 2, height // 2),
            (width, height)
        ]
    ])

    mask = np.zeros_like(image)

    cv2.fillPoly(
        mask,
        polygon,
        255
    )

    masked_image = cv2.bitwise_and(
        image,
        mask
    )

    return masked_image
```

사용:

```
roi = region_of_interest(edges)
```

전체:

```
import cv2
import numpy as np


def region_of_interest(image):

    height = image.shape[0]
    width = image.shape[1]

    polygon = np.array([
        [
            (0, height),
            (width // 2, height // 2),
            (width, height)
        ]
    ])

    mask = np.zeros_like(image)

    cv2.fillPoly(mask, polygon, 255)

    masked_image = cv2.bitwise_and(
        image,
        mask
    )

    return masked_image


img = cv2.imread("road.jpg")

gray = cv2.cvtColor(
    img,
    cv2.COLOR_BGR2GRAY
)

blur = cv2.GaussianBlur(
    gray,
    (5, 5),
    0
)

edges = cv2.Canny(
    blur,
    50,
    150
)

roi = region_of_interest(edges)

cv2.imshow("Edges", edges)
cv2.imshow("ROI", roi)

cv2.waitKey(0)
cv2.destroyAllWindows()
```

---

# 8. Hough Transform로 선 찾기

이제 Edge 이미지에서 실제 직선을 찾습니다.

사용하는 함수는:

```
cv2.HoughLinesP()
```

입니다.

```
lines = cv2.HoughLinesP(
    roi,
    1,
    np.pi / 180,
    50,
    minLineLength=50,
    maxLineGap=100
)
```

각 파라미터는 처음에는 이렇게 이해하면 됩니다.

```
1
→ 거리 해상도

np.pi / 180
→ 각도 해상도 1도

50
→ 선으로 판단할 최소 투표 수

minLineLength=50
→ 너무 짧은 선은 제외

maxLineGap=100
→ 끊어진 선도 일정 거리까지 연결
```

---

# 9. 검출된 선 그리기

별도의 빈 이미지를 만듭니다.

```
line_image = np.zeros_like(img)
```

그리고 검출한 선을 그립니다.

```
if lines is not None:

    for line in lines:

        x1, y1, x2, y2 = line[0]

        cv2.line(
            line_image,
            (x1, y1),
            (x2, y2),
            (0, 255, 0),
            5
        )
```

결과는 대략 이렇게 됩니다.

```
     /
    /
   /
         \
          \
           \
```

---

# 10. 원본 이미지와 합치기

마지막으로 원본 도로 이미지 위에 검출된 선을 겹칩니다.

```
result = cv2.addWeighted(
    img,
    0.8,
    line_image,
    1,
    0
)
```

---

# 최종 코드

여기까지 합치면 다음과 같습니다.

```
import cv2
import numpy as np


def region_of_interest(image):

    height = image.shape[0]
    width = image.shape[1]

    polygon = np.array([
        [
            (0, height),
            (width // 2, height // 2),
            (width, height)
        ]
    ])

    mask = np.zeros_like(image)

    cv2.fillPoly(
        mask,
        polygon,
        255
    )

    masked_image = cv2.bitwise_and(
        image,
        mask
    )

    return masked_image


img = cv2.imread("road.jpg")

# 1. Grayscale
gray = cv2.cvtColor(
    img,
    cv2.COLOR_BGR2GRAY
)

# 2. Blur
blur = cv2.GaussianBlur(
    gray,
    (5, 5),
    0
)

# 3. Edge Detection
edges = cv2.Canny(
    blur,
    50,
    150
)

# 4. ROI
roi = region_of_interest(edges)

# 5. Hough Transform
lines = cv2.HoughLinesP(
    roi,
    1,
    np.pi / 180,
    50,
    minLineLength=50,
    maxLineGap=100
)

# 6. 검출된 선 표시
line_image = np.zeros_like(img)

if lines is not None:

    for line in lines:

        x1, y1, x2, y2 = line[0]

        cv2.line(
            line_image,
            (x1, y1),
            (x2, y2),
            (0, 255, 0),
            5
        )


# 7. 원본 이미지와 합성
result = cv2.addWeighted(
    img,
    0.8,
    line_image,
    1,
    0
)


cv2.imshow("Original", img)
cv2.imshow("Edges", edges)
cv2.imshow("ROI", roi)
cv2.imshow("Lane Detection", result)

cv2.waitKey(0)
cv2.destroyAllWindows()
```

## 이 프로그램에서 가장 중요한 부분

코드를 외우기보다는 아래 흐름을 이해하세요.

```
cv2.imread()
     ↓
이미지 읽기

cv2.cvtColor()
     ↓
흑백

cv2.GaussianBlur()
     ↓
노이즈 제거

cv2.Canny()
     ↓
경계선 추출

ROI
     ↓
도로 부분만 선택

cv2.HoughLinesP()
     ↓
직선 검출

cv2.line()
     ↓
차선 표시

cv2.addWeighted()
     ↓
원본 + 차선 합성
```

특히 자율주행 관점에서는 이렇게 연결됩니다.

```
Camera
  ↓
OpenCV
  ↓
Lane Detection
  ↓
차선 위치
  ↓
차선 중심 계산
  ↓
조향 방향 결정
  ↓
Control
```

예를 들어 차선 중심이 카메라 중심보다 왼쪽이면:

```
차선 중심        카메라 중심
    ●---------------│

→ 왼쪽으로 조향
```

반대로 오른쪽이면:

```
카메라 중심     차선 중심
    │---------------●

→ 오른쪽으로 조향
```

여기부터가 `Vision → Control` 연결입니다.

다음 단계에서는 이 예제를 개선해서 **왼쪽 차선과 오른쪽 차선을 각각 하나의 선으로 정리하고, 차선 중앙을 계산해서 차량이 좌회전/직진/우회전 중 무엇을 해야 하는지 출력하는 프로그램**으로 발전시키면 좋습니다.