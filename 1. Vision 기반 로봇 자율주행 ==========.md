##### 자율주행 시스템 전체 구조

| **센서**    →      인식        →     위치추정     →     경로계획     →      제어
>       **Vision**/Perception       **Localization**         **Planning**             **Control**
>       
> **ROS는  이 전체 모듈을 연결하는 미들웨어/플랫폼**

```
┌──────────────┐
│    Sensor    │
│   센서 입력   │
└──────┬───────┘
       │
       │ Camera / LiDAR / IMU / Encoder
       ▼
┌──────────────┐
│  Perception  │
│  Vision/인지  │
└──────┬───────┘
       │
       │ 객체 탐지 / 장애물 / 차선 / 특징점
       ▼
┌──────────────┐
│ Localization │
│   위치 추정   │
└──────┬───────┘
       │
       │ Pose (x, y, θ) / SLAM / Sensor Fusion
       ▼
┌──────────────┐
│   Planning   │
│   경로 계획   │
└──────┬───────┘
       │
       │ Path / (global/local) Trajectory
       ▼
┌──────────────┐
│   Control    │
│   주행 제어   │
└──────┬───────┘
       │
       │ 속도 / 조향 명령 PID / PP / Staniey / MPC 
       ▼
┌──────────────┐
│  Actuation   │
│ 모터 / 로봇   │
└──────┬───────┘
       │ Moter / Wheel Steering

      ↕ 전체 시스템 연결
┌─────────────────────────┐
│           ROS           │
│ Topic / Node / Service  │
│ TF / Message / Nav2     │
└─────────────────────────┘
```

## 1. Sensor — 주변 정보를 수집

자율주행의 **입력 데이터**를 만드는 단계입니다.

|센서|역할|대표 데이터|
|---|---|---|
|Camera|영상 획득|Image|
|LiDAR|주변 물체까지 거리 측정|Point Cloud / LaserScan|
|IMU|가속도, 회전 측정|가속도, 각속도|
|Encoder|바퀴 회전량 측정|이동거리|
|GPS|실외 위치 측정|위도/경도|
|Ultrasonic|근거리 장애물 측정|거리|

예를 들어 AMR이 공장 안을 이동한다면,

```
Camera → 앞에 사람이 있는지 확인
LiDAR  → 벽/장애물까지 거리 측정
Encoder → 바퀴가 얼마나 회전했는지 측정
IMU → 로봇이 어느 방향으로 회전했는지 측정
```

이런 여러 센서 데이터를 합쳐 사용하는 것을 **Sensor Fusion(센서 융합)**이라고 합니다.

---

## 2. Vision / Perception — 주변 환경을 인식

센서가 데이터를 **수집**했다면 Perception은 그 데이터의 **의미를 파악**합니다.

```
Camera Image
     ↓
OpenCV / CNN / YOLO
     ↓
객체 검출
     ↓
사람 / 박스 / 차량 / 장애물
```

예를 들어 카메라 영상에

```
[작업자]       [박스]
   ↓             ↓
person 0.97    box 0.92
```

처럼 객체가 존재한다고 판단할 수 있습니다.

대표 기술은 다음과 같습니다.

```
OpenCV
 ├─ 이미지 전처리
 ├─ Edge 검출
 ├─ 특징점 검출
 └─ 영상 변환

Deep Learning
 ├─ CNN
 ├─ YOLO → Object Detection
 ├─ Segmentation
 └─ Depth Estimation
```

따라서 **Vision은 Perception의 일부**라고 생각하는 것이 더 정확합니다.

---

# 3. Localization — "나는 지금 어디에 있는가?"

자율주행에서 매우 중요한 단계입니다.

로봇이 다음과 같은 지도를 가지고 있다고 해보겠습니다.

```
             목적지
               ★
┌──────────────────┐
│                  │
│       장애물      │
│       ████       │
│                  │
│  🤖              │
└──────────────────┘
```

로봇은 먼저 자신의 위치를 알아야 합니다.

이를 보통 **Pose**라고 합니다.

```
Pose = Position + Orientation

Position
x = 3.2m
y = 1.7m

Orientation
θ = 45°
```

즉,

```
Pose = (x, y, θ)
```

라고 간단하게 생각할 수 있습니다.

### Localization에 사용되는 정보

```
LiDAR ──────┐
Camera ─────┤
IMU ────────┼→ Localization → Pose(x,y,θ)
Encoder ────┤
GPS ────────┘
```

여기에서 자주 등장하는 것이 **Odometry**와 **SLAM**입니다.

### Odometry

바퀴 Encoder 등을 이용하여 이동량을 계산합니다.

```
이전 위치
(x=1, y=1)

바퀴가 1m 이동
     ↓

현재 위치 추정
(x=2, y=1)
```

하지만 바퀴가 미끄러지는 등의 이유로 오차가 계속 누적됩니다.

### SLAM

**Simultaneous Localization And Mapping**

쉽게 말하면

> **지도를 만들면서 동시에 내 위치도 알아내는 기술**

입니다.

```
LiDAR / Camera
       ↓
특징점 / 장애물 검출
       ↓
SLAM
 ┌──────────────┐
 │ Map 생성     │
 │     +        │
 │ Pose 추정    │
 └──────────────┘
```

대표적으로 LiDAR SLAM, Visual SLAM 등이 있습니다.

---

# 4. Planning — "어디로 갈 것인가?"

현재 위치를 알았으므로 목적지까지 **어떤 경로로 이동할지** 결정합니다.

```
현재 위치                         목적지
   🤖                               ★
    └────────────┐
                 │
          █████  │ 장애물
          █████  │
                 └──────────────→ ★
```

Planning은 크게 두 단계로 나누면 이해하기 쉽습니다.

### Global Planning

전체 지도에서 목적지까지 큰 경로를 계산합니다.

```
START
  ↓
  └──────→──────→──────┐
                       ↓
                     GOAL
```

대표 알고리즘:

```
Dijkstra
A*
Hybrid A*
```

### Local Planning

실제 이동 중 나타나는 장애물을 피합니다.

```
계획된 경로
──────────────→

       사람
        👤
        ↑
      장애물

로봇 → ↘
        ↘ 회피
          ↗
─────────→
```

즉,

**Global Planner = 전체적인 길 찾기**

**Local Planner = 실제 주행하면서 장애물 피하기**

라고 생각하면 됩니다.

---

# 5. Path Planning과 PP(Pure Pursuit)

질문에 적으신 **PP는 Pure Pursuit**를 의미하는 것으로 보입니다.

여기서 한 가지 구분이 중요합니다.

Planning에서 다음과 같은 **Path**가 만들어졌다고 해보겠습니다.

```
● → ● → ● → ● → ● → ●
                  ↑
                목적지
```

Pure Pursuit는 이 경로상의 조금 앞쪽 점을 바라보면서 로봇이 따라가도록 만드는 **경로 추종(Path Tracking) 알고리즘**입니다.

```
                  ● Lookahead Point
                /
              /
            /
          🤖
       현재 로봇
```

로봇 바로 앞의 점이 아니라 일정 거리 앞의 **Lookahead Point**를 선택하고,

```
현재 Pose
    +
목표 Point
    ↓
Pure Pursuit
    ↓
조향각 계산
```

을 수행합니다.

따라서 엄밀하게 구분하면 PP는 **Planning 자체라기보다 Control/Path Tracking 쪽에 더 가깝습니다.**

---

# 6. Control — "실제로 어떻게 움직일 것인가?"

Planning 결과를 실제 로봇의 **속도와 방향**으로 변환합니다.

예를 들어

```
Planning

목표 경로
   ↓
Path Tracking
   ↓
Pure Pursuit / PID
   ↓
Control Command

linear velocity  = 0.5 m/s
angular velocity = 0.2 rad/s
   ↓
Motor Controller
   ↓
Left Motor / Right Motor
```

ROS의 이동 로봇에서는 흔히 다음과 같은 속도 명령을 사용합니다.

```
cmd_vel

linear.x
angular.z
```

예를 들어

```
linear.x  = 0.5
angular.z = 0
```

이면 앞으로 이동하고,

```
linear.x  = 0.3
angular.z = 0.5
```

이면 전진하면서 회전하는 식입니다.

제어 알고리즘으로는 **PID, Pure Pursuit, Stanley, MPC(Model Predictive Control)** 등이 사용됩니다.

---

# 7. ROS — 전체 시스템을 연결

여기서 ROS의 위치를 정확하게 이해하는 것이 중요합니다.

ROS는

```
Sensor → Vision → Localization → Planning → Control → ROS
```

라기보다는 다음처럼 이해하는 것이 좋습니다.

```
┌──────────────────── ROS ────────────────────┐

 Sensor
    ↓
Perception
    ↓
Localization
    ↓
Planning
    ↓
Path Tracking
    ↓
Control
    ↓
Motor

└─────────────────────────────────────────────┘
```

각 기능을 **ROS Node**로 만들고 **Topic/Message**를 통해 데이터를 주고받게 합니다.

예를 들어:

```
Camera Node
    │
    │ /camera/image
    ▼
Vision Node
    │
    │ /detected_objects
    ▼
Planning Node
    │
    │ /path
    ▼
Control Node
    │
    │ /cmd_vel
    ▼
Motor Driver
```

그래서 ROS를 **로봇의 여러 프로그램을 연결해주는 통신/통합 플랫폼**이라고 이해하면 좋습니다.

---

## 전체 흐름을 한 문장씩 정리하면

|단계|핵심 질문|주요 기술|
|---|---|---|
|**Sensor**|주변에 어떤 정보가 있는가?|Camera, LiDAR, IMU, Encoder|
|**Perception**|주변에 무엇이 있는가?|OpenCV, CNN, YOLO|
|**Localization**|나는 어디에 있는가?|Pose, Odometry, AMCL|
|**Mapping / SLAM**|주변 지도는 어떻게 생겼는가?|LiDAR SLAM, Visual SLAM|
|**Planning**|어디로 갈 것인가?|A*, Dijkstra, Local Planner|
|**Path Tracking**|계획된 길을 어떻게 따라갈까?|Pure Pursuit, Stanley|
|**Control**|모터를 어떻게 움직일까?|PID, MPC, cmd_vel|
|**ROS / ROS2**|이 기능들을 어떻게 연결할까?|Node, Topic, Service, TF|

따라서 공부할 때 머릿속에 다음 **핵심 5문장**을 잡아두면 이후 ROS2나 자율주행 알고리즘을 배우기가 훨씬 편합니다.

```
① Sensor       → 데이터를 얻는다.
② Perception   → 주변에 무엇이 있는지 알아낸다.
③ Localization → 내가 어디 있는지 알아낸다.
④ Planning     → 어디로 갈지 결정한다.
⑤ Control      → 실제 로봇을 움직인다.

ROS2 → ①~⑤를 서로 연결한다.
```

**학습순서**

[[4. 머신러닝(Machine Learning)|머신러닝]]
  ↓
학습이란 무엇인가?
분류 / 회귀
훈련 / 테스트 데이터
손실함수
과적합
성능평가
  ↓
신경망 Neural Network
  ↓
딥러닝(Deep Learning)
  ↓
CNN
  ↓
OpenCV + CNN
  ↓
YOLO 등 객체 탐지
  ↓
자율주행 Vision
