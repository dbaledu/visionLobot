
### 관계도
![[Pasted image 20260914220236.png|634]]
- ANN(Artifical Neural Network) : 인공신경망 전체를 부르는 큰 개념
- MLP(Multi-Layer Perceptron) : ANN의 한 종류(은닉층이 있는 기본 신경망)
- DNN(Deep Neural Network) : MLP 중에서도 은닉층이 깊은(여러 개인) 신경망

---
### ANN(인공신경망: Artificial Neural Network)

기본 구성 요소
- 뉴런  = 노드 = 퍼셉트론
	- 입력신호를 계산하는 기본단위. 여러 입력을 결합해 다음 단계의 판단에 필요한 출력을 생성
- 레이어 = 층
- 가중치 = weights


---
### Perceptron

퍼셉트론 = ANN의 가장 기본적인 뉴런

#### 개요
	- 시각과 뇌의 기능을 모델화한 학습 기계. 학습 기능과 지적 동작 기능을 가진 패턴 인식 기계
	- 1957년 프랑크 로젠블라트가 고안한 최초의 인공 신경망 알고리즘
	- 다수의 입력값을 받아 하나의 결과(0 or 1)을 출력하는 단순한 선형 분류기

#### 구조와 동작원리
- 입력값(X) : 퍼셉트론으로 들어오는 데이터값
- 가중치(W) : 입력값이 결과에 미치는 중요도를 조절하는 값
- 편향(Bias) : 뉴런이 얼마나 쉽게 활성화될지 조절하는 기준값
- 활성화 함수 : 입력값과 가중치를 곱한 총합이 정해진 기준(역치)을 넘으면 1, 넘지 못하면 0을 출력하는 계단 함수 역할)
- 은닉층이 없음

#### 주요 특징과 한계
- 논리 연산 학습 -> 간단한 직선 형태의 분류 문제를 해결
- XOR 문제의 해결 -> 직선 하나로 풀 수 없는 비선형 문제인 XOR 연산을 해결하지 못해 한동안 인공지능 연구가 정체
- 다층 퍼셉트론(MLP) : 단층 퍼셉트론의 한계를 극복하기 위해 여러 층을 쌓은 다층 퍼셉트론 형태로 발전 했으며 이것이 오늘날의  딥러닝 기술의 근간이 됨

---

### 1. MLP(다층 퍼셉트론 : Multilayer Perceptron) — 기본 신경망

- 가장 기본적인 형태입니다.
- 은닉층이 1개 이상
- 은닉층은 데이터를 처리하면서 **특징(feature)을 찾아내는 역할**
- 각 뉴런이 다음 층의 뉴런들과 연결되는 **Fully Connected(Dense) layer **가 기본입니다.

```
입력층 → 은닉층 → 은닉층 → 출력층
```

![[Pasted image 20260914214304.png|545]]

![[Pasted image 20260914214817.png]]


예를 들어 자동차의 데이터를 가지고 종류를 분류한다면:

```
속도 ─┐
무게 ─┼→ [신경망] → 승용차
크기 ─┤            트럭
출력 ─┘            버스
```

# 핵심 용어 정리

| 용어             | 의미                 | 비유       |         | 수식      |
| -------------- | ------------------ | -------- | ------- | ------- |
| **Layer**      | 신경망의 한 층           | 건물의 층    |         |         |
| **Dense**      | 완전연결층              | 한 층의 사무실 |         |         |
| **Unit**       | 뉴런 하나              | 심사위원 한 명 |         |         |
| **Weight**     | 가중치. 연결의 중요도(업무비중) | 심사기준     | 입력마다 하나 | wx      |
| **Bias**       | 편향. 기본 점수          | 기본 가산점   | 뉴런마다 하나 | +b      |
| **Activation** | 출력 방식 결정(활성화 함수)   | 최종 판단 기준 |         | f(wx+b) |

| 용어               | 의미                          | 역할               |
| ---------------- | --------------------------- | ---------------- |
| **가중치 Weight**   | 입력을 얼마나 반영할지 나타내는 값         | 모델이 가지고 있는 학습 대상 |
| **기울기 Gradient** | 가중치를 어느 방향으로 얼마나 바꿀지 알려주는 값 | 가중치 업데이트 방향      |
| **Loss**         | 예측이 얼마나 틀렸는지                | 현재 모델의 오차        |
| **Optimizer**    | Gradient를 이용해 가중치를 수정하는 방법  | 실제 업데이트 수행       |

#### Dense의 의미

- Dense라는 단어는 조밀한, 촘촘한이라는 뜻
- 모든 뉴런이 서로 촘촘하게 연결되어 있다는 의미
- **완전연결층(Fully Connected Layer)을 만드는 레이어**

![[Pasted image 20260914224029.png]]


#### Layer와 Dense의 관계

**Layer** (층)  
│  
├── **Dense** (완전연결층)   ← 가장 많이 사용  
├── Flatten (1차원으로 펼침)  
├── Conv2D (합성곱층)  
├── MaxPooling2D (풀링층)  
├── LSTM (순환층)  
└── Dropout (과적합 방지층)  


#### Dense의 계산 과정

```python
Dense(units=5, activation='relu')
```

```python
# 각 뉴런은 다음과 같은 계산을 합니다.
z = w1x1 + w2x2 + b
# 입력이 2개라면  ( 2 X 5 ) + 5 = 15     # 15개의 파라미터 필요
#               (입력X각뉴런가중치) + 편향      

# 그리고 활성화 함수를 적용합니다.
y = ReLU(z)
```

입력 → 가중치(Weight) → 편향(Bias) → 활성화 함수(Activation) → 출력

#### Dense 구조

**Dense** layer  
    ├── Unit(뉴런) 5개  
    ├── Weight(가중치)  
    ├── Bias(편향)  
    └── Activation(활성화 함수)  

Dense(5) 는 단순히 뉴런 5개를 만드는 것이 아니라 뉴런 5개 , 가중치, 편향, 활성화 함수(ReLU)를 하나의 층으로 묶어 놓은 것



### 가중치와 편향

- 가중치는 각 입력이 얼마나 더 중요한지를 나타내는 값. 더 중요한 입력값에 가중치를 더 지정
- gre(대학원 입학시험) 와 gpa(학부 GPA 평균학) 를 이용하여 admin(입학여부) 예측 하는 경우에 GRE×0.8+GPA×0.2 처럼 중요한 입력값에 가중치를 지정할 할 수 있다.
- 편향(Bias)은 기본점수 또는 출발점. z=wx+b 에서 x(입력값)에 w(가중치)를 곱하고 b(기본점수)를 더 줄 수 있다.

![[Pasted image 20260914231631.png|314]]

Dense(units=3, activation='relu')

![[Pasted image 20260914231735.png]]

각 뉴런은
1. 입력을 받습니다.
2. 가중치를 곱합니다.
3. 편향을 더합니다.
4. ReLU를 적용합니다.
5. 다음 층으로 전달합니다.

#### 활성화 함수

- 가중치와 편향으로 계산한 값은 그대로 사용하지 않고, 활성화 함수가 **최종 출력값**을 결정.
- 수식은 f(wx+b)
- 활성화 함수가 없다면 아무리 층을 많이 쌓아도 선형계산되고 선형회귀와 비슷해짐
- 활성화 함수가 있어야 곡선, 복잡한 경계, 이미지 패턴 같은 **비선형 문제**를 학습 할 수 있음
- 이전에는 Sigmoid와 Tanh는 출력층뿐만 아니라 은닉층에서도 사용했습니다. 현재는 은닉층에는 ReLU가 표준으로 사용됨
- Sigmoid/Tanh → ReLU로 바뀌었다기보다는, "은닉층에서는 ReLU 계열이 유리해졌고, 출력층에서는 문제의 목적에 따라 Sigmoid/Softmax/Linear를 계속 사용한다"

#### 대표 활성화 함수

1. Linear
	1. f(z) = z
	2. 그대로 출력
	3. **회귀 출력층**에 사용

2.  Sigmoid
	1. σ(z)=1 / (e−z1)
	2. 출력은 0 에서 1 사이​ 
	3. **이진 분류 출력층**에 사용
![[Pasted image 20260914232954.png|246]]

3. tanh

4. ReLU
	1. ReLU(z)=max(0,z)
	2. 음수는 0, 양수는 그대로 출력
	3. **은닉층의 기본 활성화 함수** 로 내부 특징 추출
	
![[Pasted image 20260914233650.png|272]]

5. Softmax
	1. **다중 분류 출력층**에 사용
	2. [2.5, 1.2, 0.7]  -> [0.71, 0.19, 0.10]   setosa 71%  versicolor 19% virginica 10% 가장 높은 확률은  setosa 가 됨
	3. 모든 출력값의 합은 1이됨

#### 활성화 함수의 변천 과정

Linear        : 입력값을 그대로 출력. 회귀 문제의 출력층. 예)자동차의 속도를 예측
  ↓
Sigmoid     : 그래프는 S자 형태. 0~1사이. 이진분류 출력층. 기울기 소실 문제. DNN 학습이 안됨 
  ↓
Tanh          : 시그모이드 단점을 개선.  -1 에서 1사이
  ↓
ReLU          : 
  ↓
Leaky ReLU / ELU
  ↓
GELU / SiLU(Swish) 등

| 시기     | 은닉층에서 주로 사용       | 문제                          |
| ------ | ----------------- | --------------------------- |
| 초기     | Step              | 미분/학습 어려움                   |
| 이후     | **Sigmoid**       | Gradient Vanishing          |
| 이후     | **Tanh**          | 개선됐지만 Gradient Vanishing 존재 |
| 딥러닝 발전 | **ReLU**          | Gradient 문제를 크게 개선          |
| 이후     | Leaky ReLU, ELU 등 | Dying ReLU 개선               |
| 최근     | GELU, SiLU 등      | 부드러운 학습 특성                  |

#### 기울기 소실 문제
- 시그모이드에서 입력이 아주 크거나 작으면 그래프가 평평해짐. 기울기가 0 에 가까워짐
- 신경망이 깊어지면 gradient가 여러 층을 거치면서 계속 작아지면서 결국 앞쪽 층의 가중치가 제대로 학습되지 않는 문제가 발생합니다.

#### Loss, Gradient, Optimizer

##### Loss(손실함수)
- Loss(손실)는 모델의 예측이 정답과 얼마나 다른지를 나타내는 숫자
- 회귀문제에서는 **MSE**를 사용

##### Gradient(기울기)
- 현재 가중치를 조금 변경했을 때 Loss가 어떻게 변하는지를 알려주는 값
- 이 가중치를 어느 방향으로 움직여야 Loss가 작아지는가?
- 산 중간에 있다고 가정한다면 어느 방향으로 내려가야 하는지 알려 주는 것

##### Optimizer(최적화 알고리즘)
- Gradient가 방향을 알려주면 실제로 weight를 변경하는 함수
- 대표적임 옵티마이저
	- SGD
	- Adam
	- RMSprop
	- Adagrad

##### 3가지 개념을 연결한 신경망 학습 과정

입력 데이터
    ↓
신경망
    ↓
예측값
    ↓
Loss 계산                    얼마나 틀렸지?
    ↓
Gradient 계산               어떻게 고쳐야 하지? (기울기)
    ↓
Optimizer                     실제로 어떻게 고치지   
    ↓
Weight / Bias 업데이트
    ↓
다시 예측
    ↓
Loss 계산
    ↓
반복

**신경망 학습이란 Loss를 점점 작게 만드는 방향으로 Weight와 Bias를 계속 수정하는 과정**

Loss는 틀린 정도를 측정하고, Gradient는 Weight를 어느 방향으로 바꿀지 알려주며, Optimizer는 그 Gradient를 이용해 Weight와 Bias를 실제로 업데이트한다.

그리고 여기서 **Backpropagation(역전파)**이 등장합니다.  
Backpropagation은 **Loss에서부터 거꾸로 Gradient를 계산해서 각 Weight와 Bias가 얼마나 책임이 있는지를 찾아내는 과정**이라고 이해하면 됩니다.

| 용어                | 역할               | 쉽게 말하면       |
| ----------------- | ---------------- | ------------ |
| **Weight**        | 학습되는 값           | 모델의 연결 강도    |
| **Bias**          | 학습되는 값           | 기본값/보정값      |
| **Loss**          | 예측 오차 계산         | 얼마나 틀렸나?     |
| **Gradient**      | Loss의 변화 방향 계산   | 어떻게 고쳐야 하나?  |
| **Optimizer**     | Weight/Bias 업데이트 | 실제로 어떻게 고칠까? |
| **Learning Rate** | 업데이트 크기 조절       | 얼마나 많이 고칠까?  |


---
### 2. DNN — 깊은 신경망

**Deep Neural Network**는 말 그대로 층이 깊은 신경망을 넓게 부르는 용어입니다.

```
입력
 ↓
은닉층
 ↓
은닉층
 ↓
은닉층
 ↓
은닉층
 ↓
출력
```

따라서 MLP의 은닉층을 많이 쌓으면 **DNN 형태의 MLP**라고 볼 수 있습니다.

중요한 점은 **DNN은 특정 구조 하나의 이름이라기보다 "깊은 신경망"이라는 큰 개념**이라는 것입니다.
