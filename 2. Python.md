
- [[2.1 NumPy]]
- pandas
- [[2.2 시각화 라이브러리 =====]]
	- matplotlib
	- seaborn

## 난수 생성

실험할 때마다 난수가 달라지면 결과도 조금씩 달라질 수 있습니다. 그래서 **같은 실험 결과를 재현하기 위해 seed를 고정**

> 같은 seed → 같은 난수열


예전 NumPy 코드에서는 이런 형태를 많이 볼 수 있습니다.

```
np.random.seed(1)
x = np.random.rand(5)
```

최근 NumPy에서는 다음과 같은 방식을 권장합니다.

```
rng = np.random.default_rng(1)
x = rng.random(5)
```

- `default_rng()` 방식은 난수 생성기를 별도의 객체로 관리하기 때문에 프로그램이 커졌을 때 난수 상태를 관리하기가 더 좋습니다.
- 특히 앞으로 OpenCV나 머신러닝 예제를 보다 보면 `random_state=42`, `seed=42`,  `default_rng(42)` 같은 표현이 계속 등장하는데, 기본 목적은 모두 **실험의 재현성(reproducibility)** 확보라고 이해하면 좋습니다.

## 자료형
|타입|크기|특징|
|---|--:|---|
|`np.int32`|항상 4 byte|32비트 고정|
|`np.int64`|항상 8 byte|64비트 고정|
|`np.int_`|플랫폼 의존|NumPy 기본 정수|
|Python `int`|가변|큰 정수도 자동으로 처리|
```
NumPy 숫자 타입
│
├─ 정수
│   ├─ int8
│   ├─ int16
│   ├─ int32  ← 4 byte
│   └─ int64  ← 8 byte
│
├─ 부호 없는 정수
│   └─ uint8  ← 이미지에서 매우 중요 (0~255)
│
└─ 실수
    ├─ float32 ← OpenCV/딥러닝에서 중요
    └─ float64
```

- 데이터의 범위가 작다면 `int32`를 사용해서 메모리를 절약
- `uint8`은 **부호 없는(unsigned) 8비트 정수**이므로 `0~255`를 표현할 수 있습니다. 그래서 일반적인 이미지의 RGB/BGR 픽셀 값을 저장하기에 딱 맞습니다.

## list

**flatten()과 ravel() 차이**
`flatten()`과 매우 비슷하지만 중요한 차이가 있습니다. `flatten()`은 항상 새로운 배열을 복사해서 만들지만, `ravel()`은 가능하면 원본 데이터를 공유하는 **view**를 반환합니다.