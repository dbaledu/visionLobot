인공신경망(AI)에서 입력 데이터의 가중치 합을 출력 신호로 변환할 때 사용

- Sigmoid
- ReLU(렐루: Rectified Linear Unit)
- Softmax

#### 시그모이드
- 역할: 입력값을 0과 1 사이의 부드러운 곡선 값으로 변환
- 특징: 값이 커질수록 기울기가 0에 가까워줘 학습이 멈추는 기울기 소실(Vanishing Gradient) 문제 발생
- 쓰이는 상황: 이진 분류 모델의 최종 출력층

#### ReLU
- 역할: 입력값이 0보다 작으면 0을 출력하고, 0보다 크면 입력값 그대로 출력
- 특징: 연산이 매우 단순하여 학습속도가 빠르고 기울기 소실 문제를 해결
- 쓰이는 상황: 심층신경망(DNN, CNN)의 은닉층(Hidden Layer), 현재 가장 기본으로 쓰이는 필수 함수

#### Softmax
- 역할: 입력값들을 모두 0과 1 사이로 변환하되, 모든 출력값의 총합이 1이 되도록 조절
- 특징: 출력을 확률(Probability)로 해석할 수 있게 만듦
- 쓰이는 상황: 다중 분류 모델의 최종 출력층

