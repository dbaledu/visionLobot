```
Matplotlib
   ↑
   ├── Seaborn
   │
   └── Pandas.plot()
```

**Matplotlib이 가장 기본이 되는 시각화 라이브러리**이고, Seaborn과 `pandas.plot()`은 Matplotlib을 기반으로 더 편리하게 사용할 수 있도록 만든 라이브러리

> `pandas.plot()`은 **빠르게 확인**, Seaborn은 **데이터 분석**, Matplotlib은 **자유롭게 그리고 세밀하게 제어**

- [[#Matplotlib]]
	- 그래프의 거의 모든 요소를 직접 제어
	- 논문, 보고서, 프로젝트처럼 **그래프를 세밀하게 꾸며야 할 때** 많이 사용
- [[#Seaborn]]
	- 통계/머신러닝 데이터 분석에 편리
	- **EDA(탐색적 데이터 분석)** 단계에서 특히 많이 사용
	- **Pandas DataFrame을 분석하고 시각화**할 때 편리
- Pandas `plot()`
	- 가장 빠르고 간단. 데이터를 확인하기 위해 빠르게 그래프를 그릴 수 있다는 것
	- Pandas의 `Series`나 `DataFrame`에는 자체적으로 `plot()`이 있습니다
	- 복잡한 통계 시각화나 세밀한 디자인에서는 Seaborn이나 Matplotlib이 더 적합


##### matplotlib, seaborn , pandas.plot 비교

| 구분            | Matplotlib | Seaborn       | Pandas.plot   |
| ------------- | ---------- | ------------- | ------------- |
| 난이도           | 상대적으로 높음   | 중간            | 쉬움            |
| 목적            | 범용 시각화     | 통계/데이터 분석     | 빠른 데이터 확인     |
| DataFrame 친화성 | 보통         | 매우 좋음         | 매우 좋음         |
| 그래프 세부 조정     | ⭐⭐⭐⭐⭐      | ⭐⭐⭐⭐          | ⭐⭐            |
| 통계 시각화        | ⭐⭐⭐        | ⭐⭐⭐⭐⭐         | ⭐⭐            |
| 코드 간결성        | ⭐⭐         | ⭐⭐⭐⭐          | ⭐⭐⭐⭐⭐         |
| 머신러닝 EDA      | 사용         | **매우 많이 사용**  | 간단한 확인        |
| 기반            | 자체 라이브러리   | Matplotlib 기반 | Matplotlib 기반 |

### 머신러닝/딥러닝 적용

```
Pandas
  ↓
데이터 확인
  ↓
pandas.plot()
  │
  ├─ 빠르게 그래프 확인
  │
  ↓
Seaborn
  │
  ├─ 데이터 분포
  ├─ 이상치
  ├─ 변수 관계
  └─ 상관관계
  │
  ↓
Matplotlib
  │
  ├─ 그래프 세부 설정
  └─ 학습 결과 시각화
       ├─ loss
       └─ accuracy
```

### Matplotlib

딥러닝에서는 **학습 결과**를 Matplotlib으로 시각화

```python
plt.plot(history.history["loss"], label="train")
plt.plot(history.history["val_loss"], label="validation")

plt.xlabel("Epoch")
plt.ylabel("Loss")
plt.legend()
plt.show()
```
### Seaborn

모델을 만들기 **전** 데이터를 분석

```python
sns.histplot(data=df, x="age")
sns.boxplot(data=df, x="price")
sns.scatterplot(data=df, x="height", y="weight")
sns.heatmap(df.corr(), annot=True)
```

### pandas plot()
```python
import pandas as pd

df = pd.DataFrame({
    "year": [2022, 2023, 2024, 2025],
    "sales": [100, 130, 160, 210]
})

df.plot(
    x="year",
    y="sales",
    kind="line"
)
```